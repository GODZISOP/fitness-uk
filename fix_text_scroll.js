const fs = require('fs'); let data = fs.readFileSync('src/app/components/TransformationsSection.js', 'utf8'); data = data.replace(/viewport={{ once: true }}/g, 'viewport={{ once: false }}'); data = data.replace(/viewport={{ once: true, margin:/g, 'viewport={{ once: false, margin:'); fs.writeFileSync('src/app/components/TransformationsSection.js', data);
